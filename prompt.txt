# Task: Optimize User Display Functions for Cross-Platform Reusable Environment

You are a software engineer in a cross-platform execution environment (Linux / macOS / Windows). Core logic, data processing, and tests must work uniformly across all platforms.

## Goal
Refactor `user_display_current.py` into fast, maintainable code with one-click reusable environment. Anyone can clone, run one command, and verify everything works on Windows, macOS, or Linux.

---

## What to Improve

### Current Problems
- String concatenation in loops (O(n²), slow)
- Artificial delays (0.01s per user)
- Redundant variable assignments
- Linear search O(n) in `get_user_by_id()`
- Complex nested filter logic
- Excessive temporary strings
- No error handling, logging, or cross-platform tooling

### Expected Improvements
- **20-100x faster** (<100ms for 1000+ users)
- Type hints, docstrings, PEP 8 compliant
- Graceful error handling with logging
- One-click scripts work on all platforms
- Docker containerization included

---

## Expected Deliverables

### 1. Optimized Code
- `user_display_original.py` - Keep original (reference)
- `user_display_optimized.py` - Refactored with type hints, docstrings, error handling, logging

### 2. One-Click Testing
```bash
# Setup once, run tests forever:
bash setup.sh      # Create venv, install deps (Python 3.10+)
bash run.sh        # Run optimized module with timing
bash test.sh       # Run tests with coverage (>85%)
bash clean.sh      # Reset environment

# Windows PowerShell (same commands):
powershell -File setup.ps1
powershell -File run.ps1
powershell -File test.ps1
powershell -File clean.ps1
```

### 3. Tests (>85% coverage)
- `tests/test_user_display.py` - All functions tested
- Empty lists, missing keys, performance (<50ms/100, <100ms/1000)
- Logging markers captured, errors logged gracefully

### 4. Reusable Environment
- `requirements.txt` - Pinned versions
- Virtual environment (venv) auto-setup
- Idempotent scripts (safe to run multiple times)
- Works identically on Windows, macOS, Linux

### 5. Docker Support
```bash
docker build -t user-display-optimizer .
docker run --rm user-display-optimizer        # Run tests
docker run --rm user-display-optimizer bash run.sh
```

### 6. Documentation
- `README.md` - Quick-start, before/after comparison, cross-platform setup

---

## Project Structure

```
project_root/
├── user_display_original.py       # Original (reference)
├── user_display_optimized.py      # Optimized version
├── requirements.txt               # Python 3.10+, pytest, pytest-cov
├── setup.sh / setup.ps1           # Create venv, install deps
├── run.sh / run.ps1               # Execute module
├── test.sh / test.ps1             # Run tests + coverage
├── clean.sh / clean.ps1           # Clean environment
├── Dockerfile / .dockerignore
├── README.md
└── tests/
    ├── __init__.py
    ├── test_user_display.py       # Comprehensive tests
    └── conftest.py
```

---

## Key Improvements Needed

**In `display_users()`**:
- Replace `+=` with list + `.join()`
- Remove `time.sleep()` delays
- Add error handling, logging

**In `get_user_by_id()`**:
- Use indexed dictionary (O(1) not O(n))

**In `filter_users()`**:
- Simplify nested logic with list comprehensions

**In `export_users_to_string()`**:
- Use list + `.join()`, no temp strings

**All functions**:
- Add type hints, docstrings
- Log with `[MARKER]` format
- Handle KeyError gracefully

---

## Test Coverage Required

✅ Empty list handling
✅ Missing keys handling  
✅ Performance: 100 users <50ms, 1000 users <100ms
✅ get_user_by_id() with valid/invalid IDs
✅ filter_users() with single/multiple criteria
✅ export_users_to_string() formatting
✅ Logging captures markers
✅ Error handling logs without raising

---

## Constraints

- Preserve original (`user_display_original.py`)
- English only, Python 3.10+
- Minimal deps (pytest, pytest-cov)
- Idempotent scripts, proper exit codes
- Cross-platform (Windows, macOS, Linux)
- No hardcoded paths

---

## Performance Targets

| Operation | Target |
|-----------|--------|
| Display 100 users | <50ms |
| Display 1000 users | <100ms |
| Filter 100 users | <10ms |
| Get user by ID | <1ms |

---

## Execution Plan

1. Rename current → `user_display_original.py`
2. Create `user_display_optimized.py` with all improvements
3. Write comprehensive tests in `tests/test_user_display.py`
4. Create setup/run/test/clean scripts (Bash + PowerShell)
5. Create `requirements.txt` with pinned versions
6. Create Dockerfile
7. Write README with quick-start
8. Verify all platforms
9. Final report with metrics

**Proceed**: Rename → Optimize → Test → Script → Docker → Document → Verify
